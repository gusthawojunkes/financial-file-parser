ktor:
    application:
        modules:
            - dev.wo.ApplicationKt.module
    deployment:
        port: "$PORT:8080"

    logging:
        level:
            "$LOG_LEVEL:INFO"

app:
    profile: "$PROFILE:default"

rate-limiter:
    limitPerPeriod: 20
    refillPeriodInSeconds: 60